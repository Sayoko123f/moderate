---
import PostCard from "./PostCard.astro";
import { getPosts, type PostEntry } from "@/utils/content";

interface Props {
  posts?: PostEntry[];
}

const { posts = await getPosts() } = Astro.props;
---

{posts.map((post) => <PostCard post={post} />)}
